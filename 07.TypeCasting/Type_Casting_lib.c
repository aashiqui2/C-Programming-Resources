#include <stdio.h>
#include <stdlib.h>
float divide(int, int);
int main()
{
    // int num = 123;
    // char str[10];
    // itoa(num, str, 10);//?"123"-> 123
    // printf("%s\n", str);

    // int num = 123;
    // char str[10];
    // itoa(num, str, 10);//? 123-> "123"
    // printf("%s\n", str);

    // int x = 10, y = 4;
    // float z = divide(x, y);
    // printf("%f", z);
    return 0;
}
float divide(int a, int b)
{
    // return a / b;
    return (float)a / b;
}